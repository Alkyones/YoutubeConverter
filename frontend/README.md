# Frontend

This folder contains all frontend-related files:

- `templates/` - Django HTML templates
- `static/` - Static assets (CSS, JavaScript, images)
  - `css/` - Stylesheets
  - `images/` - Image assets

These files are served by Django and referenced in the backend settings.
